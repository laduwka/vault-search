info:
  name: rebuild
  type: http
  seq: 5

http:
  method: POST
  url: http://localhost:8080/rebuild
  body:
    type: json
    raw: '{"rebuild": "true"}'
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
