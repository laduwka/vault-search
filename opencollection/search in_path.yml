info:
  name: search in_path
  type: http
  seq: 4

http:
  method: GET
  url: http://localhost:8080/search?in_path=prod&sort=asc
  params:
    - name: in_path
      value: prod
      type: query
    - name: sort
      value: asc
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
